interface Props { canvas: | HTMLCanvasElement | null; } function ExportImageButton({ canvas, }: Props) { const handleExport = () => { if (!canvas) return; const link = document.createElement("a"); link.download = "edited-image.png"; link.href = canvas.toDataURL(); link.click(); }; return ( <button onClick={handleExport}> Export Image </button> ); } export default ExportImageButton;