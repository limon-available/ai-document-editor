interface Props { open: boolean; text: string; onClose: () => void; } function ReplaceTextModal({ open, text, onClose, }: Props) { if (!open) return null; return ( <div style={{ position: "fixed", inset: 0, background: "rgba(0,0,0,0.5)", }} > <div style={{ width: "400px", margin: "100px auto", background: "white", padding: "20px", }} > <h2>Replaced Text</h2> <p>{text}</p> <button onClick={onClose}> Close </button> </div> </div> ); } export default ReplaceTextModal;