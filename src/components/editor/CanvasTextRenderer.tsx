import { createWhiteOverlay, drawNewText, } from "../../utils/textReplacement"; interface Props { canvas: | HTMLCanvasElement | null; text: string; } function CanvasTextRenderer({ canvas, text, }: Props) { const renderText = () => { if (!canvas) return; const ctx = canvas.getContext("2d"); if (!ctx) return; createWhiteOverlay( ctx, 50, 50, 300, 50 ); drawNewText( ctx, text, 60, 80 ); }; return ( <button onClick={renderText}> Replace Text On Canvas </button> ); } export default CanvasTextRenderer;