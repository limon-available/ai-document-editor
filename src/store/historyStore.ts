import { create } from "zustand"; interface HistoryState { history: string[]; currentIndex: number; addHistory: ( value: string ) => void; undo: () => void; redo: () => void; } export const useHistoryStore = create<HistoryState>((set) => ({ history: [], currentIndex: -1, addHistory: (value) => set((state) => ({ history: [ ...state.history, value, ], currentIndex: state.currentIndex + 1, })), undo: () => set((state) => ({ currentIndex: Math.max( state.currentIndex - 1, 0 ), })), redo: () => set((state) => ({ currentIndex: Math.min( state.currentIndex + 1, state.history.length - 1 ), })), }));